{
  "name": "s4rb-open-server",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "build": "rimraf dist/ && babel src --out-dir dist && copy data/cpmu.csv dist/",
    "start": "npm run build && node dist/index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Krzysztof Kogut",
  "license": "ISC",
  "dependencies": {
    "csvtojson": "^2.0.4",
    "express": "^4.16.3"
  },
  "devDependencies": {
    "@types/express": "^4.16.0",
    "babel-cli": "^6.26.0",
    "babel-preset-env": "^1.7.0",
    "copy": "^0.3.2",
    "rimraf": "^2.6.2"
  }
}
